{
  "name": "grapheway-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/grapheway",
    "examples/*",
    "website"
  ],
  "scripts": {
    "build": "bun scripts/build-unified.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "example": "bun run examples/simple-site/server.ts",
    "cli": "bun run packages/grapheway/src/cli/cli.ts",
    "website": "bun run --cwd website dev",
    "website:build": "bun run --cwd website build"
  },
  "engines": {
    "bun": ">=1.0",
    "node": ">=22"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
